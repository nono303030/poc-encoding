import { AppLayout } from '@/components/layout/AppLayout';
import { WorkflowContent } from '@/components/steps/WorkflowContent';

const Index = () => {
  return (
    <AppLayout>
      <WorkflowContent />
    </AppLayout>
  );
};

export default Index;
